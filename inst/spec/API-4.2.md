# neurogeo API 4.2

Version 4.2 adds no public API or NGCS schema. It adds independently
reproducible scientific-validation evidence for existing spatial statistics
and models. Spatial statistics with `na_action = "omit"` reconstruct the
declared W/B/none normalization on the retained raw-weight subgraph. The
package continues to implement NGCS 3.5.
