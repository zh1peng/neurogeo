% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{ngeo_kernel_regression}
\alias{ngeo_kernel_regression}
\title{Fit explicit-bandwidth spatial kernel regressions}
\usage{
ngeo_kernel_regression(
  x,
  response,
  predictors = character(),
  bandwidth,
  distance_method = NULL,
  kernel = c("gaussian", "bisquare"),
  targets = NULL,
  support = c("none", "base"),
  cutoff = 3,
  na_action = c("fail", "omit"),
  singular = c("na", "error")
)
}
\arguments{
\item{x}{An `ngeo` dataset.}

\item{response}{One response map.}

\item{predictors}{Predictor layers.}

\item{bandwidth}{Positive distance bandwidth.}

\item{distance_method}{Explicit NGCS distance_method.}

\item{kernel}{Gaussian or compact bisquare kernel.}

\item{targets}{Optional target elements.}

\item{support}{Multiply kernel spatial_weights by explicit element support.}

\item{cutoff}{Gaussian truncation multiplier.}

\item{na_action}{Whether to fail or omit incomplete training rows.}

\item{singular}{Whether singular local designs return `NA` or fail.}
}
\value{
An `ngeo_kernel_regression` data frame.
}
\description{
Local weighted least squares uses the selected NGCS distance_method. Surface
defaults to edge geodesic distance. Gaussian kernels are explicitly
truncated at `cutoff * bandwidth`; bisquare support is one bandwidth.
}
\section{When to use and limitations}{

Use these interfaces when an analysis is too large for an exact dense path or
when runtime capabilities must be checked explicitly. Do not treat reaching
an iteration limit as convergence, and do not interpret a stochastic or
truncated approximation without its error and seed diagnostics.
}

\section{Units and assumptions}{

Streaming preserves input measurement units; covariance carries their product
units and regression coefficients follow response-per-predictor units.
Solvers assume finite aligned inputs, a declared tolerance and norm, stable
chunk ordering, deterministic seeds, and enforced memory/time budgets.
}

\section{Validation evidence}{

Dense-reference, chunk-invariance, convergence, seed, and resource-budget
fixtures and their evidence outputs are declared in
`inst/spec/validation-registry-6.0.csv`.
}

\examples{
\dontrun{
ngeo_kernel_regression(data, "response", "predictor", bandwidth = 10)
}
}
\references{
Saad, Y. (2003). *Iterative Methods for Sparse Linear Systems*. SIAM.
}
\seealso{
[ngeo_resource_budget()], [ngeo_solver_control()]
}
