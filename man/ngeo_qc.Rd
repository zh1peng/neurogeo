% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{ngeo_qc}
\alias{ngeo_qc}
\title{Report bounded scientific quality-control checks}
\usage{
ngeo_qc(
  x,
  support_map = NULL,
  chart = NULL,
  tolerance = 1e-08,
  max_value_cells = 1000000L
)
}
\arguments{
\item{x}{An `ngeo` object.}

\item{support_map}{Optional source-aligned `ngeo_support_map`.}

\item{chart}{Optional cortical chart name. By default all stored charts
are inspected.}

\item{tolerance}{Positive numerical tolerance for sparse topology and
support diagnostics.}

\item{max_value_cells}{Maximum values-block cells that QC may materialize
for missingness and constant-map summaries.}
}
\value{
An `ngeo_qc` object containing a machine-readable `checks` table
  and bounded optional detail tables.
}
\description{
`ngeo_qc()` complements [ngeo_validate()]. Validation enforces object
invariants; QC reports conditions that can be structurally valid but
require a scientific decision. The function never repairs or mutates its
inputs and does not materialize a values block beyond `max_value_cells`.
}
\examples{
x <- ngeo_points(
  matrix(c(0, 0, 1, 0, 2, 0), ncol = 2, byrow = TRUE),
  values = cbind(signal = c(1, 2, 3)),
  measures = ngeo_measure(spatial_semantics = "intensive")
)
qc <- ngeo_qc(x)
qc$checks
}
