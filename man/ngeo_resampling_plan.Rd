% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resampling.R
\name{ngeo_resampling_plan}
\alias{ngeo_resampling_plan}
\title{Create an explicitly authorized transform-aware resampling plan}
\usage{
ngeo_resampling_plan(
  source,
  target,
  path,
  method = NULL,
  coverage = c("error", "drop", "normalize"),
  conservation = c("strict", "normalize"),
  missing = c("error", "drop"),
  unknown = c("error", "intensive", "extensive"),
  uncertainty = c("none", "value", "value_and_mapping"),
  source_support = NULL,
  weight_variance = NULL,
  parameters = list(),
  budget = ngeo_resource_budget()
)
}
\arguments{
\item{source, target}{Source data and target-base template.}

\item{path}{An explicitly selected `ngeo_transform_path` from the exact
source coordinate_space to the exact target coordinate_space.}

\item{method}{Nearest, volume linear/trilinear, conservative surface
source-scatter barycentric, or exact axis-aligned overlap mapping.}

\item{coverage}{Geometric coverage policy: reject, retain partial
contributions, or normalize covered contributions.}

\item{conservation}{For extensive/count layers, reject non-unit allocation
or explicitly normalize it.}

\item{missing}{Reject or explicitly drop wholly unmapped source support.}

\item{unknown}{Interpretation of unknown measurement semantics.}

\item{uncertainty}{No variance output, value variance only, or value and
mapping-weight variance.}

\item{source_support}{Optional positive source support sizes.}

\item{weight_variance}{Optional target-by-source independent mapping-weight
variances, required by `uncertainty = "value_and_mapping"`.}

\item{parameters}{Named method-specific builder parameters.}

\item{budget}{Resource limits for sparse mapping and materialized results.}
}
\value{
An immutable-identity `ngeo_resampling_plan`.
}
\description{
The plan consumes an already-supplied transform path. It never estimates
registration, resolves path ambiguity, or resamples until a later call
explicitly sets `authorize = TRUE`.
}
\section{When to use and limitations}{

Use this method when changing, comparing, or quantifying a declared
neuroimaging support. Do not equate interpolation with new information, and
do not use an atlas or resampling map whose source/target spaces, masks, or
element identities do not match the data.
}

\section{Units and assumptions}{

Measurement units follow the measure contract: intensive measures are
support-weighted, whereas extensive measures conserve totals when the map
permits it. Coordinates and distance units come from the declared spaces.
The method assumes aligned element identities, finite non-negative mapping
weights, explicit missing-support behavior, and geometry appropriate for the
requested volume, surface, grayordinate, or parcellation operation.
}

\section{Validation evidence}{

Executable support-map, conservation, resampling, and atlas conformance
suites and their evidence outputs are declared in
`inst/spec/validation-registry-6.0.csv`.
}

\examples{
\dontrun{
ngeo_resampling_plan(source_data, target_base, method = "nearest")
}
}
\references{
Glasser, M. F. et al. (2013). The minimal preprocessing pipelines for the
Human Connectome Project. *NeuroImage*, 80, 105--124.
}
\seealso{
[ngeo_support_map()], [ngeo_validate()]
}
