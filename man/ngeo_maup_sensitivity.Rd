% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experimental-multilayer-gis-61.R
\name{ngeo_maup_sensitivity}
\alias{ngeo_maup_sensitivity}
\title{Profile support-scale and zoning sensitivity}
\usage{
ngeo_maup_sensitivity(
  x,
  support_maps,
  targets,
  outcome,
  predictor,
  scale,
  zoning,
  statistic = c("correlation", "slope"),
  budget = ngeo_resource_budget()
)
}
\arguments{
\item{x}{Source `ngeo` object.}

\item{support_maps}{Named list of complete source-to-target support maps.}

\item{targets}{One matching target `ngeo` per support map, or one shared
target object.}

\item{outcome, predictor}{Single numeric layer selectors.}

\item{scale, zoning}{Character identifiers aligned with `support_maps`.
Each scale-zoning combination must be unique; scale order is the order of
first appearance.}

\item{statistic}{Support-weighted correlation or slope.}

\item{budget}{Hard execution limits from [ngeo_resource_budget()].}
}
\value{
An `ngeo_maup_sensitivity` containing support-specific estimates,
  a descriptive sums-of-squares decomposition, pairwise reversals, and
  target-region contributions that sum to each estimate.
}
\description{
Computes a support-weighted association for each declared support map and
separates variability between scales from variability among zonings within
scale. This is a descriptive MAUP sensitivity profile, not a test that an
atlas is invariant or a license to select a support on the same data.
}
\section{When to use and limitations}{

Use this method when changing, comparing, or quantifying a declared
neuroimaging support. Do not equate interpolation with new information, and
do not use an atlas or resampling map whose source/target spaces, masks, or
element identities do not match the data.
}

\section{Units and assumptions}{

Measurement units follow the measure contract: intensive measures are
support-weighted, whereas extensive measures conserve totals when the map
permits it. Coordinates and distance units come from the declared spaces.
The method assumes aligned element identities, finite non-negative mapping
weights, explicit missing-support behavior, and geometry appropriate for the
requested volume, surface, grayordinate, or parcellation operation.
}

\section{Validation evidence}{

Executable support-map, conservation, resampling, and atlas conformance
suites and their evidence outputs are declared in
`inst/spec/validation-registry-6.0.csv`.
}

\examples{
\dontrun{
ngeo_maup_sensitivity(source_data, support_maps, targets, "outcome", "predictor", scale, zoning)
}
}
\references{
Glasser, M. F. et al. (2013). The minimal preprocessing pipelines for the
Human Connectome Project. *NeuroImage*, 80, 105--124.

Fotheringham, A. S. and Wong, D. W. S. (1991). The modifiable areal unit
problem in multivariate statistical analysis. *Environment and Planning A*,
23, 1025--1044.
}
\seealso{
[ngeo_support_map()], [ngeo_validate()]
}
