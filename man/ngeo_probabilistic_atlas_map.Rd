% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support-builders.R
\name{ngeo_probabilistic_atlas_map}
\alias{ngeo_probabilistic_atlas_map}
\title{Build a support map from aligned probabilistic atlas memberships}
\usage{
ngeo_probabilistic_atlas_map(
  source,
  probabilities,
  region_id = colnames(probabilities),
  target = NULL,
  coverage = c("auto", "complete", "partial"),
  tolerance = 1e-10,
  source_support = NULL
)
}
\arguments{
\item{source}{Source dataset.}

\item{probabilities}{Source-element by region non-negative membership
matrix.}

\item{region_id}{Region identifiers, defaulting to matrix column names.}

\item{target}{Optional parcellation target. A target is stored in `map$target`
when omitted.}

\item{coverage}{Complete, partial, or infer from column sums.}

\item{tolerance}{Numerical membership tolerance.}

\item{source_support}{Optional positive source support.}
}
\value{
A sparse probabilistic or overlapping `ngeo_support_map`.
}
\description{
Build a support map from aligned probabilistic atlas memberships
}
\section{When to use and limitations}{

Use this method when changing, comparing, or quantifying a declared
neuroimaging support. Do not equate interpolation with new information, and
do not use an atlas or resampling map whose source/target spaces, masks, or
element identities do not match the data.
}

\section{Units and assumptions}{

Measurement units follow the measure contract: intensive measures are
support-weighted, whereas extensive measures conserve totals when the map
permits it. Coordinates and distance units come from the declared spaces.
The method assumes aligned element identities, finite non-negative mapping
weights, explicit missing-support behavior, and geometry appropriate for the
requested volume, surface, grayordinate, or parcellation operation.
}

\section{Validation evidence}{

Support-map, conservation, resampling, and atlas validation targets are
recorded in `inst/validation/claim-evidence-matrix-6.0.csv`. Executable
conformance suites and their evidence outputs are declared in
`inst/spec/validation-registry-6.0.csv`.
}

\examples{
source <- ngeo_point(
  matrix(c(0, 0, 1, 0, 2, 0), ncol = 2, byrow = TRUE)
)
probability <- matrix(
  c(1, 0, 0.5, 0.5, 0, 1),
  nrow = 3, byrow = TRUE,
  dimnames = list(NULL, c("A", "B"))
)
atlas <- ngeo_probabilistic_atlas_map(
  source, probability, source_support = rep(1, 3)
)
atlas
ngeo_support_diagnostics(atlas)
}
\references{
Glasser, M. F. et al. (2013). The minimal preprocessing pipelines for the
Human Connectome Project. *NeuroImage*, 80, 105--124.
}
\seealso{
[ngeo_support_map()], [ngeo_validate()]
}
