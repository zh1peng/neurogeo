# Design records

This directory contains development plans, audits, risks, decisions, and
frozen historical rationale.

The canonical package contracts are in `../inst/spec/`. Existing same-name
documents in this directory are historical snapshots; do not create or update
a second copy of a current API, migration, format, or NGCS contract here.
Place newly superseded material in `archive/`; existing root snapshots retain
their paths only for historical-link compatibility.
