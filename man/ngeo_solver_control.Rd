% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iterative-models.R
\name{ngeo_solver_control}
\alias{ngeo_solver_control}
\title{Declare deterministic iterative solver and approximation controls}
\usage{
ngeo_solver_control(
  tolerance = 1e-08,
  max_iterations = 1000L,
  trace_order = 20L,
  trace_probes = 30L,
  seed = 3401L,
  workers = 1L,
  exact_threshold = 200L,
  on_nonconvergence = c("error", "return"),
  budget = ngeo_resource_budget()
)
}
\arguments{
\item{tolerance}{Positive relative residual tolerance.}

\item{max_iterations}{Positive solver iteration limit.}

\item{trace_order}{Positive power-series order for approximate log
determinants.}

\item{trace_probes}{Positive Hutchinson probe count.}

\item{seed}{Required deterministic probe seed.}

\item{workers}{Positive deterministic probe worker count.}

\item{exact_threshold}{Maximum dimension for explicitly requested
exact-small log determinants.}

\item{on_nonconvergence}{Raise a classed error or return diagnostics.}

\item{budget}{Hard execution resource limits.}
}
\value{
An immutable `ngeo_solver_control`.
}
\description{
Declare deterministic iterative solver and approximation controls
}
\section{When to use and limitations}{

Use these interfaces when an analysis is too large for an exact dense path or
when runtime capabilities must be checked explicitly. Do not treat reaching
an iteration limit as convergence, and do not interpret a stochastic or
truncated approximation without its error and seed diagnostics.
}

\section{Units and assumptions}{

Streaming preserves input measurement units; covariance carries their product
units and regression coefficients follow response-per-predictor units.
Solvers assume finite aligned inputs, a declared tolerance and norm, stable
chunk ordering, deterministic seeds, and enforced memory/time budgets.
}

\section{Validation evidence}{

Dense-reference, chunk-invariance, convergence, seed, and resource-budget
fixtures and their evidence outputs are declared in
`inst/spec/validation-registry-6.0.csv`.
}

\examples{
\dontrun{
ngeo_solver_control(tolerance = 1e-8, max_iterations = 1000)
}
}
\references{
Saad, Y. (2003). *Iterative Methods for Sparse Linear Systems*. SIAM.
}
\seealso{
[ngeo_resource_budget()], [ngeo_solver_control()]
}
