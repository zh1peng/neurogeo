% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experimental-point-spacetime-61.R
\name{ngeo_nonseparable_hotspots}
\alias{ngeo_nonseparable_hotspots}
\title{Map exploratory nonseparable space-time hotspots}
\usage{
ngeo_nonseparable_hotspots(
  x,
  spatial_weights,
  spatial_bandwidth = 1,
  temporal_bandwidth = NULL,
  interaction = 1,
  spatial_cutoff = NULL,
  temporal_cutoff = NULL,
  z_threshold = 1.96,
  permutations = 0L,
  seed = NULL,
  adjust = c("maxT", "none"),
  retain_matrix = FALSE,
  budget = ngeo_resource_budget()
)
}
\arguments{
\item{x}{Time-axis-enabled `ngeo` object whose value columns are time maps.}

\item{spatial_weights}{Matching spatial weights. Directed graphs are reduced
to their undirected union for hotspot neighborhoods.}

\item{spatial_bandwidth}{Positive graph-hop bandwidth.}

\item{temporal_bandwidth}{Positive time bandwidth; defaults to the median
observed interval.}

\item{interaction}{Positive nonseparable space-time interaction coefficient.}

\item{spatial_cutoff}{Maximum graph hops retained.}

\item{temporal_cutoff}{Maximum absolute time distance retained.}

\item{z_threshold}{Positive descriptive state threshold.}

\item{permutations}{Number of joint space-time null randomizations. Zero
returns descriptive scores only.}

\item{seed}{Reproducible null seed.}

\item{adjust}{Max-T familywise correction across the complete local
space-time map, or unadjusted cellwise Monte Carlo p-values.}

\item{retain_matrix}{Whether to retain the redundant space-by-time score
matrix in addition to the long-form `local` table.}

\item{budget}{Hard execution resource limits.}
}
\value{
An `ngeo_nonseparable_hotspots` with local scores, descriptive state
summaries, adjacency edges ordered by first hot time, and optional local
Monte Carlo p-values.
}
\description{
Computes local Getis--Ord-style standardized scores over graph-hop and
irregular-time neighborhoods with a positive space-time interaction term.
The calculation uses truncated neighborhood lists rather than a dense
all-pairs space-time matrix. Optional inference uses one joint action that
permutes spatial identities and declared time coordinates for the complete
space-time field, with max-T familywise control across all local cells.
State labels and temporally ordered adjacency edges remain descriptive; they
do not establish transmission or causality.
}
\section{When to use and limitations}{

Use this method only after declaring the spatial support, coordinate space,
distance or weights definition, and independent sampling unit. Do not treat
vertices, voxels, or parcels as independent subjects, and do not interpret a
spatial-map null as population inference.
}

\section{Units and assumptions}{

Measurement units are inherited from the selected measure. Distance units
come from the declared coordinate space; a graph metric is unitless unless
its edge costs carry a documented unit. Inference requires the assumptions
stated by the selected model or null, finite observations, aligned geometry,
and an analysis support that matches the spatial weights.
}

\section{Validation evidence}{

Executable simulation and conformance suites for stable statistical methods
and their evidence outputs are declared in
`inst/spec/validation-registry-6.0.csv`.
}

\examples{
\dontrun{
ngeo_nonseparable_hotspots(x, spatial_weights, spatial_bandwidth = 2)
}
}
\references{
Waller, L. A. and Gotway, C. A. (2004). *Applied Spatial Statistics for
Public Health Data*. Wiley.

Getis, A. and Ord, J. K. (1992). The analysis of spatial association by use
of distance statistics. *Geographical Analysis*, 24, 189--206.
}
\seealso{
[ngeo_spatial_weights()], [ngeo_validate()]
}
