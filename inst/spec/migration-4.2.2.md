# Migration to neurogeo 4.2.2

No user-code migration is required. Public objects, functions, NGCS schemas,
and numerical semantics are unchanged from 4.2.1.

Maintainer validation now prefers the project-local R library and uses a
download-only external fixture cache whose files are checked against an
installed immutable manifest.
