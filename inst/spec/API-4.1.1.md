# neurogeo API 4.1.1

Version 4.1.1 adds no public API or NGCS schema. Spatial-model weight
subsetting uses the staged sparse Matrix coercion path without changing
ordering, normalization, isolate handling, or numerical semantics.

The package continues to implement NGCS 3.5.
