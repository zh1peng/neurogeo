# Historical design archive

New superseded plans and audits move here. The older files already stored at
the `design/` root are retained at their existing paths to avoid breaking
historical links, but they are snapshots, not current contracts. Only
`inst/spec/` is canonical for installed API, schema, migration, validation,
condition, option, and release contracts.
