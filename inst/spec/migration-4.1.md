# Migration to neurogeo 4.1

No object migration is required from 4.0. Use
`ngeo_example_data(name)` to obtain an integrity-checked installed path for a
controlled format fixture instead of constructing a path inside another
package library.

The NGCS schema remains 3.5 and all 4.0 scientific and resource boundaries
remain in force.
