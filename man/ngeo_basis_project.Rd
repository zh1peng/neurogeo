% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basis-projection-45.R
\name{ngeo_basis_project}
\alias{ngeo_basis_project}
\title{Project aligned layers into a fixed spatial basis}
\usage{
ngeo_basis_project(
  x,
  basis,
  index = NULL,
  layers = NULL,
  bands = NULL,
  center = TRUE,
  scale = FALSE,
  summaries = c("coefficients", "absolute_energy", "relative_energy", "roughness"),
  chunk_rows = 8192L,
  chunk_layers = 32L
)
}
\arguments{
\item{x}{An aligned `ngeo` object.}

\item{basis}{A matching `ngeo_spatial_basis`.}

\item{index}{Optional `ngeo_layer_index`.}

\item{layers}{Optional layer selection.}

\item{bands}{Optional named, non-overlapping retained-mode groups.}

\item{center}{Whether to support-weight center each map.}

\item{scale}{Whether to support-weight scale each centered map.}

\item{summaries}{Projection summaries to return.}

\item{chunk_rows}{Maximum component rows read together.}

\item{chunk_layers}{Maximum layers read together.}
}
\value{
An `ngeo_subject_features` object.
}
\description{
Projection is component-, row-, and map-chunked and returns a small
independent-unit by endpoint matrix. Reconstruction is not performed
implicitly.
}
\section{When to use and limitations}{

Use these functions to inspect or change aligned values, layers, measures, or
derived subject features after element identity has been verified. Do not
treat layers as spatial elements, infer subject independence from column
count, or combine measurements with incompatible units or support behavior.
}

\section{Units and assumptions}{

Values retain the unit and intensive/extensive behavior declared by their
measure. Rows remain aligned to base elements and columns to layer IDs.
Binding, subsetting, projection, and coupling assume stable IDs, compatible
bases, explicit missingness, and one measurement contract per selected layer.
}

\section{Validation evidence}{

Alignment, layer-index, measure, chunking, projection, and coupling fixtures
are declared in `inst/spec/validation-registry-6.0.csv`; claim status is in
`inst/validation/claim-evidence-matrix-6.0.csv`.
}

\examples{
\dontrun{
ngeo_basis_project(data, spatial_basis)
}
}
\references{
Wickham, H. (2014). Tidy data. *Journal of Statistical Software*, 59, 1--23.
}
\seealso{
[ngeo_validate_layers()], [ngeo_measure()]
}
